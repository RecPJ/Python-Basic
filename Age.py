# Python Program to calculate your age
# Take inputs fromt the user
current = float(input('Enter the current year:'))
yob = float(input('Enter Year of birth:'))
# calculate the age
age = current-yob
print('Your current age is {0}'.format(age))